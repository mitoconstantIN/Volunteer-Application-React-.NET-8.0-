
import axios from "axios";

const api = "https://localhost:7096/api/event";

export const eventsAddAPI = async(symbol: string) =>{
    try{
        //const data = await axios.post<Events.EventPost>(api + '?title=${title}' + '?eventType=${eventType}')
    }catch{}
}